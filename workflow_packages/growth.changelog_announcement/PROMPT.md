Read the project context, the given repository, the optional since_run_id and audience_note
inputs. Follow the changelog-announcement skill and write one file at
content/announcements/{run_id}.md.

Do not publish, send messages, post anywhere, or change any file other than the declared
output. Do not invent merges that did not happen. If nothing merged since the last run, or
nothing merged is worth telling a user about, write the file explaining that instead of
inventing content to fill it.
