Inspect the landing page named by `site_url` for the founder-defined `conversion_goal` and
`audience`. Follow the landing-page-cro skill, use relevant project evidence and optional
`analytics_context`, and write only reports/cro/LANDING_PAGE_CRO_AUDIT.md.

Produce an evidence-backed, prioritized experiment backlog. Separate observed evidence,
interpretation, hypotheses and unknowns. Do not invent conversion metrics or statistical
significance. If evidence is insufficient, say so explicitly. Remain completely read-only:
do not modify project files, publish content, send messages, create experiments or start another
workflow.
