Follow the brand-findability skill to search the founder's brand name the way someone who only
heard it would, score whether those searches reach the site, and name at most three fixes for
the searches that lose people.

Resolve the brand, domain, category and own hosts from the inputs first and from project
context when they are empty: the onboarding plan, the latest organic audit evidence, project
memory and the keyword plan. If neither the inputs nor the project name the brand and its
site, write a diagnostic report that says which is missing and what to run, and do not search.

Build the query plan, score every result, choose fixes and compare with the newest earlier
report in `reports/brand-findability/` only through the SCORE.md functions. Search the public
web only, with one search per planned query and at most five page fetches.

Write only the declared report, ending with the fence `next_state` returned. Do not change the
website or any other file, publish, post, submit a form, open a pull request, contact anyone,
or start another workflow.
