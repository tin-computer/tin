Create one clear diagram from the founder's brief.

Read the `content-diagram` skill completely and follow its finite vocabulary and source grammar.
Use durable project files only when they add necessary context. Write exactly the output path in
the procedure contract and change no other file. The result must be plain Mermaid source, not a
Markdown fence, SVG, prose, or implementation plan.

Use the prepared diagram_brand context and read its listed BRAND.md / DESIGN.md paths. Brand guidance governs presentation and voice; design observations inform accurate product language. Copy the supplied source_line unchanged immediately after the graph header, when present.
